# Bug reproduction

The seeded defect is: running execution report is marked successful.

Affected symbol: `internal/domain/masking/report.go::Success`.

A focused regression test demonstrates the failure before the fix and passes after the fix.
